# Iaet.Explorer

**Planned — Phase 2**

`Iaet.Explorer` will host a local Swagger-like web UI that reads from the SQLite catalog and lets you browse, filter, and manually invoke captured endpoints without any external tooling dependency.

This assembly is currently a stub placeholder.
