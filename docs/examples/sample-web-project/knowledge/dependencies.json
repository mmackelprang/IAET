{
  "dependencies": [
    { "from": "POST /api/v1/auth/login", "to": "GET /api/v1/users", "reason": "Bearer token from login response" }
  ],
  "authChains": [
    {
      "name": "Session auth",
      "steps": [
        "POST /api/v1/auth/login -> access_token (JWT)",
        "Authorization: Bearer {access_token} -> all /api/v1/* endpoints"
      ]
    }
  ]
}
