{
  "name": "iaet-capture",
  "version": "0.1.0",
  "description": "IAET Capture — background fetch/XHR interceptor with popup start/stop/export",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "chrome-types": "^0.1.327",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
